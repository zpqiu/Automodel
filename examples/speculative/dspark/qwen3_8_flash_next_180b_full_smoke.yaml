# Two-node full-depth pipeline smoke for Qwen3.8-Flash-Next DSpark.
#
# Unlike the one-node diagnostic smoke, this loads all 48 target layers on an
# EP16 mesh and executes one complete frozen-target forward plus draft optimizer
# step. The tiny local dataset and draft are for validation only.

recipe: TrainDSparkRecipe

dist_env:
  backend: nccl
  timeout_minutes: 120

wandb:
  enable: false

distributed:
  strategy: fsdp2
  defer_fsdp_grad_sync: true
  tp_size: 1
  cp_size: 1
  pp_size: 1
  ep_size: 16
  activation_checkpointing: false
  moe:
    reshard_after_forward: true
    wrap_outer_model: true

recipe_args:
  target_model_name_or_path: /lustre/fs1/portfolios/coreai/projects/coreai_dlalgo_nemorl/users/shuangy/models/brightdelta-real/brightdelta-180b-bf16/brightdelta-180b-bf16_vv2
  train_data_path: examples/speculative/dspark/qwen3_8_flash_next_smoke_data.jsonl
  val_data_path: null
  output_dir: ./outputs/dspark_qwen3_8_flash_next_180b_full_smoke
  seq_length: 64
  micro_batch_size: 1
  grad_accumulation_steps: 1
  num_workers: 0
  num_epochs: 1

  draft_num_hidden_layers: 1
  block_size: 2
  num_anchors: 2
  mask_token_id: 248319
  target_layer_ids: [46]
  freeze_embeddings: true
  attention_backend: flex_attention

  chat_template: |-
    {% for message in messages %}{{ '<|im_start|>' + message['role'] + '\n' }}{% if message['role'] == 'assistant' %}{% generation %}{{ message['content'] }}{% endgeneration %}{% else %}{{ message['content'] }}{% endif %}{{ '<|im_end|>\n' }}{% endfor %}

  target_attn_backend: flex
  target_dispatcher: hybridep
  target_experts: torch_mm
  target_enable_fsdp_optimizations: true

  markov_rank: 32
  markov_head_type: vanilla
  confidence_head_alpha: 1.0
  confidence_head_with_markov: true
  ce_loss_alpha: 0.1
  l1_loss_alpha: 0.9
  loss_decay_gamma: 4.0

  trust_remote_code: false
  shuffle_seed: 42
  log_every_steps: 1
  max_grad_norm: 1.0

optimizer:
  lr: 6.0e-4
  betas: [0.9, 0.95]
  weight_decay: 0.0
  warmup_ratio: 0.0
  min_lr_ratio: 0.1

checkpoint:
  enabled: false
